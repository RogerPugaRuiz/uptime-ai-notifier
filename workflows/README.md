# Workflows Directory

This directory is for storing n8n workflow files.

You can export your workflows from n8n and place them here to version control them.

The workflows will be automatically available in n8n when you run the docker-compose services.
